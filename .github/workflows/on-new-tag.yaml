name: On New Tag

on:
  push:
    tags:
      - 'v*'

jobs:
  publish-docker-image:
    uses: ./.github/workflows/publish-docker-image.yml
    secrets: inherit
    with:
      environment: docker-publish
