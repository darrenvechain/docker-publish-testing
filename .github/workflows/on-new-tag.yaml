name: On New Tag

on:
  push:
    tags:
      - 'v*'

jobs:
  publish-docker-image:
    uses: ./.github/workflows/publish-docker-image.yaml
    secrets: inherit
    permissions:
      contents: read
      packages: write
    with:
      environment: docker-publish
